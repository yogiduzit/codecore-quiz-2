<div class="main-container">
  <% if !@ideas.nil? %>
    <div class="ideas-container">
      <% @ideas.each do |idea| %>
        <div class="idea">
          <h3 class="idea-title"><%= link_to idea.title, idea_path(idea) %></h3>
          <p class="idea-body"><%= idea.body %></p>
        </div>
      <% end %>
    </div>
  <% end %>
</div>